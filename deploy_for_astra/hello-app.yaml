apiVersion: apps/v1
kind: Deployment
metadata:
  name: astra
  namespace: default
spec:
  selector:
    matchLabels:
      app: astra
  replicas: 1
  template:
    metadata:
      labels:
        app: astra
    spec:
      containers:
      - name: astra
        images: dcksv-linux-repo.kronshtadt.ru:5000/astralinux-build-smolensk:v1
---

#apiVersion: v1
#kind: Service
#metadata:
#  name: astra-service
#  namespace: default
#  labels:
#    app: astra
#spec:
#  type: ClusterIP
#  selector:
#    app: astra
#  ports:
#  - port: 80
#    targetPort: 80
#    protocol: TCP